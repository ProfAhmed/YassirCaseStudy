<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/home_navigation"
    app:startDestination="@id/navigation_movie">
    <fragment
        android:id="@+id/navigation_movie"
        android:name="com.osama.pro.app.home.movie.MovieFragment"
        android:label="@string/nav_movie"
        tools:layout="@layout/fragment_movie" />

    <fragment
        android:id="@+id/navigation_favorite"
        android:name="com.osama.pro.features.favorite.ui.movie.MovieFragment"
        android:label="@string/nav_favorite"
        app:moduleName="The_Movie_DB.favorite"
        tools:layout="@layout/fragment_favorite" />
</navigation>